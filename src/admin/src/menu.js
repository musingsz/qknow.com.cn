export default function (nga, admin) {
    // return nga.menu()
    //     .addChild(nga.menu()
    //         .title('用户管理 ')
    //         .icon('<span class="fa fa-users fa-fw"></span>')
    //         .link('/users/list') // use the same entity list for several menu items
    //       );
}
